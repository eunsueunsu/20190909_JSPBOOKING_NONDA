# 예약 관련 SQL 파일
# 전체 조회용 쿼리
selectList = SELECT B.*, P.PTITLE TITLE, P.PCATEGORY GENRE, B.BVIEWDATE-SYSDATE DAYNUM \
			 FROM BOOKING B \
			 JOIN PROGRAM P \
			 ON (B.PCODE = P.PCODE) \
			 WHERE BID =? \
			 ORDER BY BNO